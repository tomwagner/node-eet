# Travis CI config file
# https://docs.travis-ci.com/

dist: xenial

language: node_js

node_js:
  - "11.*"
  - "10.*"
  - "9.*"
  - "8.*"
  - "7.*"

cache:
  yarn: true

install:
  - yarn

script:
  - yarn test
